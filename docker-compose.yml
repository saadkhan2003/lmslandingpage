services:
  web:
    image: ai-tools-mastery-landing:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5033:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
